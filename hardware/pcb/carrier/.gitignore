# dependencies
node_modules

# tscircuit build cache + local registry/parts cache
dist
.tscircuit
*.tsbuildinfo
.cache

# NOTE: out/ is intentionally committed — rendered PNGs + Gerbers + STEP live
# in git alongside the source, the same convention as the CAD .step / .step.png.

# misc
*.tgz
coverage
*.lcov
.env
.env.*.local
logs
*.log
.DS_Store
.idea
