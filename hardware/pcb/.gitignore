# SKiDL byproducts — regenerated by controller_board.py (the netlist itself is tracked)
*.erc
*.log
*_sklib.py

# KiCad local state + layout byproducts (the .kicad_pcb / .kicad_pro are tracked)
*.kicad_prl
*-backups/
*.kicad_pcb-bak
fp-info-cache

# autoroute round-trip (see README "Board layout" — regenerate as needed)
*.dsn
*.ses
*-drc.rpt
