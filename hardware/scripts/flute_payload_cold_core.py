"""Flute the cold core's shell and its two caps. The box's six are `flute_payload_enclosure.py`.

See that file for why the two trees are two rules.
"""

import flute_payload


if __name__ == "__main__":
    raise SystemExit(flute_payload.main(flute_payload.COLD_CORE_DIRS))
