# Published G Ganen correction

The [mount review](mounts.json) binds the published pump and mounting geometry and
the post-publication lint. The cap has no findings; the lid has six answered
features matching its tie passages and support-removal openings.

The [published assembly check](carrier-placement/published-payload.json) passes
30 checks. The six fixed pieces match their canonical surfaces; both carrier
surfaces match the exact facts-derived working position. Every other assembly
entry is unchanged by the publication refresh. Native CAD and all seven reviewed
print archives retain their qualification.

`carrier-placement/diagnosis.json` and its probe records preserve the evidence
for the publisher's explicit carrier placement. Archived Python scripts have a
`.txt` suffix; their bytes retain the hashes in the original run records. The
viewer uses the assembly facts for its carry, independent of a reduced mesh's
bounding-box approximation.
