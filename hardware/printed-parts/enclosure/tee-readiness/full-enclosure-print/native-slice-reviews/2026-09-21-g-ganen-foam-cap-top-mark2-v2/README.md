# foam-cap-top — Mark2

Private production workflow for the current G Ganen mounting geometry. The saved PET-GF profile supplies the process, supports and automatic brim. Requested trim is +0.04 mm; expected textured-plate command is +0.02 mm. Keep native +Z up, centered at X162.5/Y160 in the 325 × 320 mm left-nozzle area, with the established 15 mm model border.

No geometry is staged. Run only after the coordinating task explicitly confirms regenerated source/artifacts are ready and provides a fresh exact geometry receipt. The receipt must bind the new STEP/STL, loaded sources, Box and qualification evidence.

The top cap uses the fresh canonical fluted STL. The top lid uses the new native STEP triangulation in this variant's inputs directory. The lid input reproducer requires an explicit current cold-core generation receipt and never edits production geometry.

Prepare, slice and inspect this exact native archive independently. Review actual bed bounds, settings/trim, every support contact and removal route. Retain and assess any geometry diagnostic against current emitted paths. Earlier contact measurements and approvals are context only. The four current mounting stations and bearing surfaces must be covered by the fresh native geometry handoff.

The retained predecessor files are hash-listed in predecessor.json and remain ineligible for dispatch. These two variants replace only the two mounting plates in the queue. No command controls a printer or grants final release.
