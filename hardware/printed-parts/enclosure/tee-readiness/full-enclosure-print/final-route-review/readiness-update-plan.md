# Readiness update after final qualification

The coordinating task controls completion and release. The current three readiness
files remain pending until exact final evidence is available.

1. Read the completed producer-chain result, current full scorecard and facts.
   Require the active combined fit, port-lead, clearance and carrier-motion gates
   to pass. If a gate fails, retain the concrete failure and its exact input scope;
   do not substitute this bounded regulator probe for the aggregate result.
2. Bind the final Box, shell/carrier STEP/STL/payloads, matching mount meshes and
   sources in the fresh receipt. Use the fresh canonical 2 mm pusher proof and
   carrier/native equality, plus the current retaining-wall declaration. Rebind
   evidence when final exported bytes differ; do not inherit support approvals.
3. Update `../../../print-readiness.md` and `../../../print-readiness.json`
   with the completed build/receipt links and exact hashes. Replace the three
   pending route readings with the final observed results. Record final counts
   only from that scorecard. Geometry qualification remains distinct from slicing
   and from physical spring rigidity, retention, operating feel and cycle life.
4. Update `../queue.json` per plate from carrier_revision's fresh preparation,
   native archive and readiness_verification's support-removal ledger. Bind actual
   archive/G-code/settings/support evidence, including emitted auto-brim and working
   toolpaths. A reviewed plate can be handed off independently; other unfinished
   plate reviews do not become an extra gate for it.
5. Record printer handoff only from the coordinating task's exact launch
   observation. Preserve the running cartridge/cap archive, countertop ownership,
   accepted lever/nameplate/display fit and no-coupon/no-additional-scan scope.
   Never rewrite archived launch/cancellation records or mark physical trial
   outcomes measured before assembly.

Only `hardware/printed-parts/enclosure/print-readiness.md`, its `.json`, and
`tee-readiness/full-enclosure-print/queue.json` need the current-state update.
No production geometry or generator changes are part of that documentation task.
