# Manifold arrangement — four trays placed (first pass)

All four [tray assemblies](../) positioned in space to show how they connect.
Connecting tube runs are **not modeled**. A first arrangement to refine, not a
final stack.

- **nozzle-gate · bag-circuit · bib-gate** sit **butted plate-to-plate** in
  series along X. Each tray's pump/channel side faces the next (nozzle's
  Y-D/Y-G toward bag's V-F/V-I, bag's V-E/V-H toward bib's Y-C/Y-F); bib-gate is
  turned 180° about Z so its outlets face back toward bag. Where two trays
  meet, the protruding valve port of one nestles into the neighbor's saddle
  channel and meets its fitting.
- **source-select** rides **above** the series (its body spans over bag and
  bib), positioned so its V-C/V-D outlets sit over bib-gate's two upward Tee
  branches (~44 mm above them).

26 solids, envelope ≈ **376 × 90 × 127 mm**. STEP colors: tray tan, valves
dark, Tees orange, Y-dividers blue; the `/3d` viewer renders all gray.

Generated by `manifold_arrangement.py` → `manifold-arrangement.step`.
Regenerate with `tools/cad-venv/bin/python manifold_arrangement.py`.
