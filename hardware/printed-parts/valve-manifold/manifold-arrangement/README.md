# Manifold arrangement — four trays placed (first pass)

All four [tray assemblies](../) positioned in space to show how they connect.
Connecting tube runs are **not modeled**; the trays are spaced to leave room
for them. This is a first arrangement, not a final stack — orientations and
offsets are placeholders to refine.

- **nozzle-gate → bag-circuit → bib-gate** sit in series along X. Each tray's
  pump/channel side faces the next: nozzle's Y-D/Y-G outlets toward bag's
  V-F/V-I, bag's V-E/V-H toward bib's Y-C/Y-F. Bib-gate is turned 180° about Z
  so its outlets face back toward bag-circuit.
- **source-select** rides above bib-gate, turned 180° about Z so its V-C/V-D
  outlets sit over bib-gate's two **upward Tee branches**.

26 solids, envelope ≈ **577 × 90 × 135 mm**. STEP colors: tray tan, valves
dark, Tees orange, Y-dividers blue; the `/3d` viewer renders all gray.

Generated by `manifold_arrangement.py` → `manifold-arrangement.step`.
Regenerate with `tools/cad-venv/bin/python manifold_arrangement.py`.
