# Faucet-inlet stub — rear-panel carbonated-water pass-through (lite)

The lite edition's rear-panel pass-through for carbonated water. Water from the
external Lillium carbonator enters the rear panel, turns inward, crosses the
DIGITEN flow meter (detected flow triggers the flavor pumps), turns back, and
exits the panel to the faucet.

Flow path, port to port:

    Lillium hose
      -> bulkhead IN   (panel pass-through, +X side)
      -> elbow IN      (turns the run inward)
      -> DIGITEN flow meter   (inline, ports coaxial on +X / -X)
      -> elbow OUT     (turns the run back toward the panel)
      -> bulkhead OUT  (panel pass-through, -X side)
      -> faucet

## Parts

| Ref | Part | Source |
|---|---|---|
| flow-meter | DIGITEN G1/4" Hall-effect flow sensor (FL-S402B, Amazon B07QRXLRTH) | `/hardware/reference/digiten-flow-sensor/` |
| elbow ×2 | John Guest PP0308E 1/4" union elbow | `/hardware/reference/elbow-connector/` (McMaster stand-in) |
| bulkhead ×2 | John Guest 1/4" PTC bulkhead union | `/hardware/reference/jg-bulkhead-union/` |

## Interface

The DIGITEN flow meter presents 1/4" push-to-connect ports — the same size as
the elbows and bulkheads — so each meter port takes a 1/4" OD tube directly and
the 1/4" elbows mate it with no adapter. The meter is inline: its two ports are
coaxial on opposite rims (one +X, one −X). It lies flat with its flow axis along
X between the two bulkheads; each elbow drops from a bulkhead's inner ring face
down to the meter axis and turns in to meet that side's collet — a flat U: in at
+X, across through the meter, out at −X.

## Coordinate convention

- **Y** = panel-normal — +Y outside the enclosure (toward the Lillium hose and
  the faucet), −Y inside. The rear panel is the bulkhead seating plane at Y = 0.
- **X** = lateral — the two bulkheads straddle X = 0 at ±[49.56](BULKHEAD_X);
  the flow meter sits centered between them.
- **Z** = up.

The two bulkheads sit [99.12](BULKHEAD_PITCH) mm apart on the panel — the meter's
two collets reach 30 mm off its center along the flow axis, and an elbow leg
stands off each. The flow meter and both elbows live inside the enclosure, the
meter axis at Y = −[44.35](STUB_DEPTH_Y) mm.

## Open interface

- The flow-meter solid is photo-estimated (no manufacturer dimension drawing
  exists); swap in measured FL-S402B geometry once one is on the bench.
- The elbow is the McMaster stand-in STEP, not the production PP0308E.

Generated by `faucet_inlet_stub.py` → `faucet-inlet-stub.step`.
Regenerate with `tools/cad-venv/bin/python faucet_inlet_stub.py`.

## Sources
[value](NAME) texts are updated by:
- `/pie-in-the-sky/lite/printed-parts/faucet-inlet-stub/faucet_inlet_stub.py`
