# Faucet-inlet stub — rear-panel carbonated-water pass-through (lite)

The lite edition's rear-panel pass-through for carbonated water. Water from the
external Lillium carbonator enters the rear panel, turns inward, crosses the
DIGITEN flow meter (detected flow triggers the flavor pumps), turns back, and
exits the panel to the faucet.

Flow path, port to port:

    Lillium hose
      -> bulkhead IN   (panel pass-through, +X side)
      -> elbow IN      (turns the run inward)
      -> reducer       (3/8" PTC -> 1/4" PTC)
      -> DIGITEN flow meter   (axis along X, inside the enclosure)
      -> reducer       (1/4" PTC -> 3/8" PTC)
      -> elbow OUT     (turns the run back toward the panel)
      -> bulkhead OUT  (panel pass-through, -X side)
      -> faucet

## Parts

| Ref | Part | Source |
|---|---|---|
| flow-meter | DIGITEN G3/8" Hall-effect flow sensor (FL-308, Amazon B07QQW4C7R) | `/hardware/reference/digiten-flow-sensor/` |
| elbow ×2 | John Guest PP0308E 1/4" union elbow | `/hardware/reference/elbow-connector/` (McMaster stand-in) |
| bulkhead ×2 | John Guest 1/4" PTC bulkhead union | `/hardware/reference/jg-bulkhead-union/` |
| reducer ×2 | John Guest PP201208WP 3/8" PTC × 1/4" PTC reducing union | adapter, modeled as a stepped stub |

## Adapter interface

The DIGITEN flow meter presents 3/8" push-to-connect ports; the elbows and
bulkheads are 1/4" PTC. Each meter port mates to the rest of the 1/4" line
through a **John Guest PP201208WP** 3/8" OD × 1/4" OD push-to-connect reducing
union (Prime, polypropylene). It is modeled here as a stepped coaxial stub —
its length and diameters are estimates pending a part in hand.

## Coordinate convention

- **Y** = panel-normal — +Y outside the enclosure (toward the Lillium hose and
  the faucet), −Y inside. The rear panel is the bulkhead seating plane at Y = 0.
- **X** = lateral — the two bulkheads straddle X = 0 at ±[79.56](BULKHEAD_X);
  the flow meter spans between them along X.
- **Z** = up.

The two bulkheads sit [159.1](BULKHEAD_PITCH) mm apart on the panel — the flow
meter is 60 mm port-to-port, and a reducer plus an elbow leg stand off each end.
The flow meter, both elbows, and both reducers live inside the enclosure at
Y = −[44.35](STUB_DEPTH_Y) mm.

## Open interface

- The reducer geometry is an estimate; swap in measured PP201208WP geometry
  once one is in hand.
- The elbow is the McMaster stand-in STEP, not the production PP0308E.

Generated by `faucet_inlet_stub.py` → `faucet-inlet-stub.step`.
Regenerate with `tools/cad-venv/bin/python faucet_inlet_stub.py`.

## Sources
[value](NAME) texts are updated by:
- `/pie-in-the-sky/lite/printed-parts/faucet-inlet-stub/faucet_inlet_stub.py`
