# chain

The sessions [`Chain.md`](../Chain.md) quotes, as clean transcripts — user and assistant
turns, no tool calls. Each filename is the session's own title, so any of them is also
retrievable live:

```
python3 ~/Developer/claude-code-setup/jsonl2md/jsonl2md.py export-session "Routes 22"
```

Read `Chain.md` first. Come here for the pair at the centre of it: the same two runs, one
session sweeping and reporting a wall, the next asserting the target and walking through it.

| Session | What it holds | Find |
| --- | --- | --- |
| [Routes 22](<Routes 22.md>) | The sweep. Four builds over tray travel, a table read as a wall, three turns spent debugging the scan itself, and the revert that left the tree byte-identical. Also the bare `28.4` nobody could measure against anything. | `stop scanning` |
| [Routes 24](<Routes 24.md>) | The same two runs, asserted instead of sampled. The frozen domino was the bore's entry angle, not the tray's travel — both runs to R25.40 in one session. | `never the fence` |
| [Routes 23](<Routes 23.md>) | The campaign read from outside: one derivation behind three separately-requested moves, and the pattern Derek's own moves follow — dissolve the reason, not the run. | `one knot` |

The half-move that started the run of reverts is in the session before Routes 22, which is
not on disk here. Routes 23 reads it back at `V-H/V-I seat swap`.
