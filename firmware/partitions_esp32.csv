# Main board (ESP32-WROOM-32E-N4, 4 MB). Dual-slot: an image arrives over the
# link into the slot that is not running, and the bootloader falls back to the
# slot that still verifies if the new one does not.
#
# The update path this serves is `firmware/README.md`, "A shipped unit is
# updated from the iOS app".
#
# nvs is where the flavor selection and sound settings live, and it sits below
# the app slots — a USB reflash writes 0x1000/0x8000/0xe000/0x10000 and leaves
# it alone, so a partition change does not cost the machine its selection.
#
# app: 427 KB in a 768 KB slot.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0xC0000,
app1,     app,  ota_1,   0xD0000,  0xC0000,
spiffs,   data, spiffs,  0x190000, 0x270000,
