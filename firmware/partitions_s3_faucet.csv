# Faucet display (Waveshare ESP32-S3-Touch-LCD-1.47, 16 MB). Dual-slot, and its
# own table: `partitions_s3.csv` is the prototype's round rotary display, which
# has 8 MB and no reason to move when this board's slots change.
#
# The update path this serves is `firmware/README.md`, "A shipped unit is
# updated from the iOS app".
#
# app: 1.04 MB in a 3 MB slot.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x300000,
app1,     app,  ota_1,   0x310000, 0x300000,
spiffs,   data, spiffs,  0x610000, 0x9F0000,
