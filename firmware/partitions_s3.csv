# ============================================================================
# The prototype's 1.28" rotary display (Meshnology, 8 MB flash).
#
# TWO APP SLOTS. An image lands in the one that is not running and the boot
# partition moves only after the whole of it is in and its CRC32 matches; a
# transfer that stalls leaves the board running what it booted. The update path
# is `firmware/README.md`, "A shipped unit is updated from the iOS app": this
# board hosts the NimBLE GATT server the phone talks to, and relays over UART to
# the prototype's ESP32. The unit is never on WiFi or the internet.
#
# THE BOARD UNDER THE COUNTER IS RUNNING THE SINGLE-SLOT TABLE IT WAS FLASHED
# WITH. A partition table is the one thing an update cannot install, so it takes
# one USB visit to arrive at this one, and none after that.
#
# `art` holds the 16 animation frames and 3 flavor faces at 240x240 RGB565 —
# 2,188,832 bytes, laid out by `tools/make_art.py rotary` and read through
# `firmware/lib/board_art`. Compiled in they were 2.1 MB of a 3.2 MB app, which
# is why two slots and a filesystem did not fit around them.
#
#   0x010000  app0    1.5 MB   the app is ~1.1 MB
#   0x190000  app1    1.5 MB
#   0x310000  art     2.25 MB  holds 2,188,832 B
#   0x550000  spiffs  2.69 MB  uploaded images, 10 slots at 115,200 B
# ============================================================================
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x180000,
app1,     app,  ota_1,   0x190000, 0x180000,
art,      data, 0x40,    0x310000, 0x240000,
spiffs,   data, spiffs,  0x550000, 0x2B0000,
