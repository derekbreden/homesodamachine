# ============================================================================
# !! READ BEFORE UNIT #001'S FIRMWARE IS FLASHED FOR SHIPMENT !!
# ----------------------------------------------------------------------------
# This is a SINGLE-SLOT partition table. There is an `otadata` partition and
# an `app0` (ota_0) slot, but NO `app1` (ota_1). ESP-IDF / Arduino OTA refuses
# to write without a second slot because rollback is impossible.
#
# Architecture: updates flow phone -> BLE -> S3 (this MCU) -> UART -> ESP32
# main. The S3 hosts the NimBLE GATT server the iOS/Android apps talk to.
# The unit is never on WiFi or the internet (air gap is intentional). BLE OTA
# still needs two app slots on the device.
#
# Once a unit ships with this single-slot layout, it CANNOT be retrofitted to
# dual-slot over the air -- by definition, the OTA mechanism we'd use to push
# the fix requires the second slot to already exist. Decide BEFORE unit #001
# or accept that unit forever requires a service-visit reflash.
#
# The S3 has 8 MB of flash, so the fix is cheap. Proposed dual-slot layout,
# sketch only -- verify before committing:
#   nvs       data nvs    0x9000   0x5000
#   otadata   data ota    0xe000   0x2000
#   app0      app  ota_0  0x10000  0x200000  (2 MB)
#   app1      app  ota_1  0x210000 0x200000  (2 MB)
#   spiffs    data spiffs 0x410000 0x3F0000  (~4 MB, unchanged)
# No real cost on this MCU.
#
# Companion file: firmware/partitions_esp32.csv has the same problem on the
# 4 MB main controller (tighter fit there -- ~1 MB of LittleFS goes away).
#
# When this is done: delete this banner.
# ============================================================================
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x400000,
spiffs,   data, spiffs,  0x410000, 0x3F0000,
