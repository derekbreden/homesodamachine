# Enclosure display (Waveshare ESP32-S3-Touch-LCD-4.3B, 16 MB). Dual-slot.
#
# The update path this serves is `firmware/README.md`, "A shipped unit is
# updated from the iOS app".
#
# The loading animation is compiled in — ~4 MB of .rodata against ~1.5 MB of
# everything else — so this board's slots are the widest of the three and the
# image still fills 81% of one. Art that moved to a partition of its own would
# take both slots down to 2 MB; until it does, a slot here is 7 MB.
#
# app: 5.65 MB in a 7 MB slot.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x700000,
app1,     app,  ota_1,   0x710000, 0x700000,
spiffs,   data, spiffs,  0xE10000, 0x1F0000,
